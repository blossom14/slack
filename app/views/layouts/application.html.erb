<!DOCTYPE html>
<html>
  <head>
    <title>Chat</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  
  </head>

  <body>
  <nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">
      ㅅㅅㄷㅌ</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/friendships">친구랑 대화하기</a></li>
        <li><a href="/messages">모두와 대화하기</a></li>
        <% if user_signed_in? %>
        <li class = "profile"><%= current_user.username %></li>
          <li><%=link_to "로그아웃", destroy_user_session_path, method: :delete, data: {confirm: "Are you sure?"} %></li>
        <% else %>
          <li class = "profile"><p><%=link_to "로그인", new_user_session_path %></p></li>
        <% end %>
      </ul>  
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div class="fluid-container" style="margin-top: 70px">
  <div class="row">
    <div class="col-sm-2" style="position: fixed">
      <% if user_signed_in? %>
        <h5>Chatrooms</h5>
        <ul>
          <% current_user.chatrooms.each do |chatroom| %>
            <li><%= link_to chatroom.name, "#" %></li>
          <% end %>
        </ul>
      <% end %>
    </div>
    
    <div class="col-sm-10 col-sm-offset-2">
    <%= yield %>
    </div>
  </div>
</div>

</body>
</html>